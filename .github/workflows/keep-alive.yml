name: Keep Alive Ping

on:
  schedule:
    # Run once daily at 12:00 UTC
    - cron: '0 12 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl -f "${{ secrets.SITE_URL }}/api/ping" || exit 1
